name: Build APK - Simple Method

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Create Simple APK
      run: |
        echo "🎯 Creating minimal APK structure..."
        
        # ایجاد یک APK ساده
        mkdir -p simple-apk/
        
        # ایجاد فایل‌های ضروری APK
        cat > simple-apk/AndroidManifest.xml << 'EOF'
        <?xml version="1.0" encoding="utf-8"?>
        <manifest package="com.kohksh.game">
            <application android:label="Kohksh Game">
                <activity android:name=".MainActivity">
                    <intent-filter>
                        <action android:name="android.intent.action.MAIN"/>
                        <category android:name="android.intent.category.LAUNCHER"/>
                    </intent-filter>
                </activity>
            </application>
        </manifest>
        EOF
        
        echo "✅ Simple APK structure created"
        
    - name: Upload Simple APK
      uses: actions/upload-artifact@v4
      with:
        name: kohksh-simple
        path: simple-apk/
